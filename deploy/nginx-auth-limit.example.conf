# Optional nginx rate limit for auth endpoints (generic example).
# Include http {} block in nginx.conf; server {} block in your vhost.

# --- http context ---
# limit_req_zone $binary_remote_addr zone=learnai_auth:10m rate=20r/m;

# --- server context ---
# location ~ ^/api/v1/auth/(login|2fa/verify|register)$ {
#     limit_req zone=learnai_auth burst=10 nodelay;
#     proxy_pass         http://APP_UPSTREAM_API:8000;
#     include            /etc/nginx/snippets/proxy-headers.conf;
#     proxy_read_timeout 60s;
# }
